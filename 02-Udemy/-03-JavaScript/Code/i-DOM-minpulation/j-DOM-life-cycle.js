//---------
document.addEventListener("DOMContentLoaded", function (e) {});
// this event wait for the HTML and js to be loaded

document.addEventListener("load", function (e) {
  // this event wait for the HTML CSS  images and all Assets to be loaded
});

// document.addEventListener("beforeunload", function (e) {
// this event wait for the user to leave the page
// this event happen just before the page is closed

// e.preventDefault()
// to make that we don't leave the page
// e.returnValue = "";
// });
