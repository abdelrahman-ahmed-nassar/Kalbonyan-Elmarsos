/*
*--------- rendering list of data 
using an array of object then pass it into the child and render it using map

*------------ key prop 
to set a unique value to every element

you should use key when mapping such an list of item

*---------- conditional content 
use ternary operator to make condition expression inside the curly braces

OR 

use the and logical operator to make a like a guard clause

OR 

make the condition out of the jsx

*---------- auto style 
style={object}
the key must the property name in camel case 
or is surrounded by quotes 

the value must be the value
*/
